# ── Backend RUNTIME dependencies (deploy / serve only) ───────────────────────
# The FastAPI request path is ML-free: it reads pre-computed JSON artifacts into
# memory (no database, no pandas/lightgbm/catboost at runtime). These three are
# all that's needed to SERVE the API — keep this list minimal so cloud builds
# (e.g. Render) are fast and reliable.
#
# For local data regeneration / ML / tests, install the full `requirements.txt`.

fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0
